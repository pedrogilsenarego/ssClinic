const OrpheusProRegular = ({ children }: any) => {
  return (
    <p
      style={{
        fontFamily: "'orpheuspro', sans-serif;",
        fontSize: "55px",
      }}
    >
      {children}
    </p>
  );
};

export default OrpheusProRegular;
